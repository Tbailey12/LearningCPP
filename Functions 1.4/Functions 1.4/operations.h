//this is where the header guard goes, this stops it from being used more than once
#ifndef OPERATIONS_H
#define OPERATIONS_H
//this is the content of the header file, where the declarations go

//this is a function prototype, it allows us to call the add function whenever we want
int add(int x, int y);
int multiply(int, int);
int doubleNumber(int);

#endif //end of the header guard